const Thing = struct {
    const Self = This();

    fn new(self: Self) {
        self + i32
    }
};

fn Vec(T: type) {
    struct {
        t: T + main(),//
        y: foo(),
    }
}

fn print(s: string) {
    struct {
        fn fmt(io: Io) {
            // print the stuff
        }
    }.fmt
}

fn main() {
    // stuff
    print(
        // "hello"
    )(io)
}

fn foo() {

}
